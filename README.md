# Horma+ Landing Page

## Технологии

- **Frontend**: React, Vite, Tailwind CSS
- **Backend**: Django

## Ссылка
hormaplus.vercel.app

# Local and production secrets

Before starting Docker Compose, copy `.env.example` to `.env` and set a unique
`SECRET_KEY`. The `.env` file is intentionally ignored by Git. Never put real
tokens, passwords, certificates, or private keys in tracked files.
