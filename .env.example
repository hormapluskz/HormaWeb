# Copy this file to .env and replace the placeholder before deploying.
# .env is ignored by Git and must never be committed.
SECRET_KEY=replace-with-a-long-random-django-secret-key

# Used only if the contact form sends Telegram notifications.
TELEGRAM_BOT_TOKEN=
